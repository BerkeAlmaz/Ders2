using System;

namespace DegerVeReferansTiplerr
{
    class Program
    {
        static void Main(string[] args)
        {
            //int, devimal, float, double, bool = değer tip -- bellekte stack
            //array, class, interface = referans tip -- bellekte heap
        }
    }
}
